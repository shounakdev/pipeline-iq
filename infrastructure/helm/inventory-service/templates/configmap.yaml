apiVersion: v1
kind: ConfigMap
metadata:
  name: inventory-service-config
  labels:
    app: inventory-service
data:
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
